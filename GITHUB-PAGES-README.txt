ROGUE GALLERY — GitHub Pages

Upload the contents of this ZIP to the root of your GitHub Pages repository.
The repository root must contain index.html.

Firebase configuration and Firestore rules are included as provided in the project source.

After publishing, hard-refresh the site if an older PWA version is cached.
